//this function will return one if the string has exactly n characters
#include <stdio.h>
#include <string.h>

char s[] = "hello";
int n = 5;



int main(){
(char *s, int n){

    int total = strlen(s);
    if (total == n){
        return 1;
        }
    return 0;
        
    }

