//this function will return one if the string has exactly n characters
#include <stdio.h>
#include <string.h>

int main(){
    

    char s[] = "hello";
    int n = 5;
    int total = strlen(s);
    if (total == n){
        printf("1");
        }
    return 0;
        
    }

